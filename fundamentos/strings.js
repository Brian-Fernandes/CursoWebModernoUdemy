const escola = 'Cod3r'
/*  
    Devolve a letra que está no indice indicado(x)
    começando pelo indice 0
    Se o indice não for "Achado" retornará vazio
*/
console.log(escola.charAt(4))

/* 
    * Retorna se contém este indice na operação
*/
console.log(escola.indexOf('3'))

/*
    *Retorna um array com os valores separando-os por uma vírgula
*/
console.log('Ana,Maria,Pedro'.split(','))